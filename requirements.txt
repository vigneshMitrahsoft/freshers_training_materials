# Freshers Training Materials - Python dependencies
# Tested on Python 3.12 (use Python 3.10-3.12).
# Install with:  pip install -r requirements.txt

# --- Notebook runtime (required to open & run any .ipynb) ---
notebook==7.2.2
ipykernel==6.30.1
nbconvert==7.17.1

# --- Web frameworks & APIs ---
Django==5.2
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-filter==24.3
fastapi==0.111.0
uvicorn[standard]==0.30.6
pydantic==2.13.4
Flask==3.0.2

# --- Databases / ORM ---
SQLAlchemy==2.0.41
psycopg2-binary==2.9.10   # PostgreSQL driver (needs a running PostgreSQL server)
PyMySQL==1.1.1            # MySQL driver (needs a running MySQL server)

# --- Data / big data ---
numpy==1.26.4
pandas==2.2.1
matplotlib==3.8.3
pyspark==3.5.1            # requires Java 8/11/17 installed and on PATH

# --- Security / misc ---
PyJWT==2.8.0
cryptography==42.0.5
requests==2.32.3

# --- AI / LLM POC (AI/11.poc_knowledge_assistant.ipynb) ---
groq==0.11.0                     # Groq LLM API client (free key at https://console.groq.com)
sentence-transformers==3.0.1     # local embedding model (first run downloads ~90MB)
python-dotenv==1.0.1             # load GROQ_API_KEY from a .env file
